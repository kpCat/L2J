# Independent final review — Goal 017

Статус: `ACCEPT_WITH_EXPLICIT_FUTURE_CONTRACTS`

Принятый финальный commit:
`6cf261370e3cb98158805828e995cfe6e8b14651`.

Exact parent:
`0015a5ffd0c10a99514732ef52b969a39ac62eb7`.

Subject:
`fix(phantoms): finalize party terminal verification`.

Финальный micro-completion подтвердил reusable terminal formation, безопасную
границу party pulse `10..10000`, ACTIVE-gated exact command goals, bounded
coordinator lifecycle, один frozen-tree `ant verify`, standalone `ant jar` и
test-only исправление ожидания terminal population materialization.

Goal 017 принят без нового suffix. Явные будущие контракты:

- social memory остаётся downstream observer canonical Party facts;
- свежий managed consent требует exact ACTIVE `party.join`;
- social persistence failure не откатывает canonical Party;
- language, clans, PvP, economy и matchmaking не входят в Goal 017.

Зафиксированные статусы:

- Goal 016: `ACCEPT_WITH_EXPLICIT_FUTURE_CONTRACTS`;
- Goal 017: `ACCEPT_WITH_EXPLICIT_FUTURE_CONTRACTS`;
- Goal 018: `IMPLEMENTED_PENDING_INDEPENDENT_REVIEW`;
- Goal 019/020/021/025: `NOT_STARTED`.

Historical verifier 017 обязан проверять blobs/scope принятого commit и
допускать любой текущий HEAD, для которого принятый commit является ancestor.

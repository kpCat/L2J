# Goal034 closure 4 acceptance

PASS iff managed=10 terminal/unique; per-profile durable schedules are evaluated at captured instants; actual online is a subset of desired ACTIVE and count equals `min(activeTarget=5, maxMaterialized, desiredActiveCount)` with expected count >0; real LS/GS READY+registered; native restart/drain succeeds; gen2 retains identities/ecology and independently satisfies schedule admission; exact cleanup/no orphans/integrity; verify/jar green; production DB unused; Goal034 current docs=SUCCESS; exact commit + non-force push complete.

/goal Выполни corrective Goal 024A из docs/phantoms/tasks/024a-farming-agreement-lifecycle-corrections/TASK.md на ветке feature/phantom-world, required parent 2603776c6996007b147f93e4c7e79f145ceb8a89. Goal 024 пока CHANGES_REQUIRED; Goal 025+ не начинать. Сохрани принятый Goal024 kernel и закрой только R024A-01/02/03. R024A-01: до bilateral FINAL remaining/progress/acquisition/social evidence — mutable arbitration truth, поэтому drift должен инвалидировать/recompute draft; после exact bilateral FINAL remaining/progress остаются historical evidence и обычный monotonic Goal021 progress НЕ должен ломать SHARE/WAIT/MOVE — live binding проверяет pair + goal/revision + source + ResourceKey + authority + exactPair + TTL, не equality current remaining. R024A-02: fresh Goal010 perceptibility требуется для старта новой negotiation, но exact begun pair обязан иметь persisted bounded causal perception receipt; after OFFER/final one-hop visibility может исчезнуть без стирания exact agreement до TTL. После restart loser-first обязан exact-load/revalidate persisted counterpart by ID и НЕ получать ALLOW только потому, что holder ещё не получил scheduler pulse; никаких profile/listProfiles/World scans. R024A-03: manual observeAgreementOutcome(..., boolean) не считать production proof. Встрой evidence-driven reconciliation: реальный Goal021 MOVE проходит существующий SWITCH/switchSource exactly once, затем Goal024 наблюдает source change, освобождает old claim и bilateral FULFILLED + Goal018 agreement.fulfilled; WAIT переживает progress holder и fulfils на holder completion/release/move; SHARE переживает progress; TTL => EXPIRED; authority drift => STALE; BROKEN только по объективному exact breach evidence, иначе не выдумывать. Social record failure должен иметь durable idempotent retry truth. Если state layout меняется — schema v2 с safe v1 legacy-untrusted handling, no SQL. Обязательные dynamic tests: SHARE-after-progress, WAIT-after-progress, drift after OFFER/RESPONSE, perceptibility lost after OFFER/final, loser-first restart before holder pulse, real Goal021 MOVE switch+automatic fulfillment, expiry, social retry, terminal bilateral fault matrix и old Goal024 regressions. Никакого искусственного лимита по числу файлов: pre-audited set + justified exact call-path expansion. No other chronicles, production DB, .l2j, global scans, workers/Futures, direct chat, PvP/combat. Seed 24002402. После focused 024A + original 024 aggregate + exact Goal010/017/018/020/021/023C regressions + historical verifiers 023/023A/023B/023C/024 + working verifier 024A — один final 024A aggregate, freeze, один plain ant verify, ant jar, ordinary commit/push и byte-identical verifier 024A PS5.1/verified PS7. Commit "fix(phantoms): harden farming agreement lifecycle". При полном успехе выведи GOAL_024A_FARMING_AGREEMENT_LIFECYCLE_CORRECTED_PENDING_INDEPENDENT_REVIEW.

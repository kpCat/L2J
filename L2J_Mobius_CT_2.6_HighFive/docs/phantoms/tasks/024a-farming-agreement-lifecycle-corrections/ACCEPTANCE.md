# Goal 024A acceptance

## A. Baseline/status

Require exact parent `2603776c6996007b147f93e4c7e79f145ceb8a89`, branch `feature/phantom-world`, Goal024 CHANGES_REQUIRED entering task, R024A-01/02/03 open, Goal025+ NOT_STARTED. Do not self-ACCEPT Goal024.

## B. Mutable evidence / stable final binding

PASS only if pre-final progress/evidence drift invalidates or recomputes proposal; after bilateral final normal progress does not invalidate SHARE/WAIT/MOVE; live final binding no longer compares current remaining to historical remaining; historical remaining remains arbitration evidence.

## C. Causal perception

New negotiation requires fresh bounded Goal010 perceptibility. Persisted exact causal receipt continues exact pair for bounded TTL, cannot authorize other pair/resource/goal, and topology drift fails closed. No fourth topology signal source.

## D. Loser-first restart

Loser may be only profile advanced; holder needs no scheduler pulse; exact pair lazily revalidated by counterpart ID; WAIT stays WAIT, MOVE stays MOVE; missing runtime peer never creates ALLOW; no scan.

## E. Production agreement resolution

Manual boolean reporting is not authoritative. Dynamic proof: real Goal021 MOVE -> source switch -> old FULFILLED; WAIT survives progress then fulfills on release/completion/move; SHARE survives progress; expiry EXPIRED; authority drift STALE; BROKEN only objective exact violation evidence.

## F. Bilateral terminal truth

FULFILLED/BROKEN/EXPIRED/STALE converge exact on both components; one-sided terminal write has no contradictory effect; restart mirrors same ID/status.

## G. Goal018 history

Fulfilled/broken events come from production reconciliation, deterministic IDs, failed social writes retain retry truth, eventual delivery once, EXPIRED/STALE never emit broken.

## H. Goal021 ownership

Farming never writes selected Source, MOVE stays through existing Goal021 SWITCH, no direct switchSource call from farming, old claim released after observed source change, new resource fresh claim.

## I. Goal020 facts

Live agreement survives progress and query reports current remaining; stale/expired/one-sided suppressed; no language in farming domain.

## J. Existing Goal024 architecture preserved

Resource identity, current-perception discovery, same-Party SHARE, five acts, three-claimant convergence, no PvP/global scans/workers, profile-component persistence and strict policy remain true.

## K. Schema/restart

If schema changes: deterministic v2 codec, safe v1 handling, no SQL, legacy state cannot authorize stale effect.

## L. Dynamic tests

All materially distinct TEST_CASES pass dynamically. Source contains checks are not proof.

## M. Historical regression matrix

Run current exact targets for original Goal024 focused/affected aggregate, Goal010, Goal017, Goal018, Goal020, Goal021, Goal023C and disabled Phantom gate. Historical verifier 024 descendant-compatible without weakening original pinned semantics.

## N. No artificial file count

Verifier 024A must not encode numerical changed/new/read file ceiling. Exact scope allowlist is acceptable if it does not block justified call-path expansion.

## O. Final verification discipline

After freeze: focused 024A, original Goal024 aggregate, affected regressions, historical verifiers 023/023A/023B/023C/024, working verifier 024A, one final 024A aggregate, one plain ant verify, one standalone ant jar, freeze unchanged, ordinary commit/push, post-commit verifier PS5.1 + verified PS7 byte-identical stdout. Repeat full verify only after genuine relevant correction and explain.

Success token:

```text
GOAL_024A_FARMING_AGREEMENT_LIFECYCLE_CORRECTED_PENDING_INDEPENDENT_REVIEW
```

Goal025 remains NOT_STARTED until independent review.

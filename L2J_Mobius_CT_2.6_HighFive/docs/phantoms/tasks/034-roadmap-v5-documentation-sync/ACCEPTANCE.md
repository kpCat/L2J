# Roadmap v5 documentation sync — acceptance

PASS iff all conditions are true:

- branch/HEAD/origin precondition соответствует TASK;
- изменены только разрешённые documentation files + этот task package + новый report;
- production Java/XML/config/schema/test code не менялись;
- DB/server/Ant build/test commands не запускались;
- `PHANTOM_DEVELOPMENT_MASTER_PLAN.md` содержит Roadmap v5 finite tail Goal034..Goal039;
- `docs/PHANTOM_BOTS_ROADMAP.md` имеет version 5 и тот же authoritative future tail;
- `docs/phantoms/PHANTOM_CURRENT_STATUS.md` указывает closure6 как текущий Goal034 report и честно фиксирует Goal034 `BLOCKED`, gen2 latest `5/5/1`;
- `docs/phantoms/NEW_DIALOG_START_MESSAGE.txt` больше не отправляет следующий диалог в closure4/v4 и содержит актуальный closure6 + Roadmap v5;
- Goal037 канонически требует 100% quest-script inventory и multi-level rates QA;
- Goal038 канонически требует humanized RU social/off-topic conversation, contextual profanity, user custom overrides и optional mature opt-in OFF by default;
- Goal039 — единственный final full-vision release gate + freeze;
- после Goal039 ACCEPT записан `FEATURE_COMPLETE_FOR_DECLARED_SCOPE` и запрет automatic Goal040+;
- historical Goal030/Goal031... reports/statuses не переписаны задним числом;
- closure6 production/runtime findings не искажены;
- следующий runtime step явно указан как новый Goal034 resume от post-restart gen2 1/5 evidence;
- documentation consistency scan PASS;
- mojibake / escaped Cyrillic exact-scope scan PASS;
- `git diff --check` PASS;
- exact-path commit + non-force push выполнены;
- user-owned staged/unstaged/untracked files не затронуты.

/goal Выполни задачу из docs/phantoms/tasks/010b-topology-signal-ledger-bounds/TASK.md. Закрой только bounded signal ownership: замени unbounded sequences map и pending-cleanup set одним capped per-profile ledger с тремя fixed sources; capacity учитывает active, retained и failed-cleanup identities; never-owned inactive targetability не создаёт state и не вызывает scheduler; re-registration сохраняет monotonic sequences; ledger освобождается только при all-three NOT_REGISTERED либо final stop; STALE cleanup успешен только при локально доказанном inactive source, а impossible submit statuses fail closed. Не меняй topology XML, generation ordering, loaders, scheduler/navigation/decision semantics, config/schema и не начинай Goal 011/012. Выполни signal-ledger/generation/perception/core ×3, corpus/performance, все regressions, verify/jar, ordinary commit/push и верни TOPOLOGY_SIGNAL_LEDGER_BOUNDED_PENDING_INDEPENDENT_REVIEW.

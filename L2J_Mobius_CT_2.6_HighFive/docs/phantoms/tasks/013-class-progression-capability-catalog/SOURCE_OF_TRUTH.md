# SOURCE OF TRUTH — Goal 013

Order:

1. current High Five server code and loaded runtime config;
2. current High Five datapack loaders and XML;
3. accepted Game Knowledge IDs/hashes;
4. strict curated progression metadata with validated stable IDs;
5. external retail claims as documented cross-checks only;
6. player practice/recommendations as non-mechanical doctrine.

Primary sources:

- PlayerClass, CategoryData, ClassListData;
- SkillTreeData, SkillData, SkillLearn, RequestAcquireSkill;
- Player/VillageMaster/SubClassHolder;
- ItemData and canonical equip path;
- PetDataTable/NpcData/Summon/Servitor/Pet/BabyPet;
- PlayerConfig and current config files.

Names are never identity. Current server implementation wins when an external
retail claim differs.

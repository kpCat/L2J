# LIVE-002-BAND-40-51 — bounded factual path search

Дата: 24.09.2026. Статус: **BLOCKED**. Ветка `feature/phantom-world`, обязательный tracked baseline `0957c67c62306af77106b07e72734c5050593e73` совпал с HEAD до изменений; upstream `origin/feature/phantom-world`. Итоговый commit SHA указан в сообщении о доставке, поскольку SHA самого commit не может быть записан в содержащийся в нём файл.

## Результат и точный blocker

GLOBAL ordinary `40–51` остался `final_reachable=0`. Единственный заявленный missing-edge reason: **`NO_GEODATA_PATH`** для направленного destination connector `dest.3a51136c3a2c22a3bfc9a6d9 → generated.farm.4c20d26f8b57611bdafd0d33.anchor` (`connector.e2b2d66973960044d8058892`). Native NORMAL NPC 30540, transition `transition.5e4e0e2c12a78f3d80185ef1` в Abandoned Coal Mines (139714, -177456, -1536; feeId 57, feeCount 690) и уже admitted source connector `connector.b0c14ba43d9b0dcc750959ab` дают конкретную доступную цепочку до destination. Реальный `PhantomTravelGeoProbe` вернул для последнего перехода `NO_PATH`, `path_length=0`, `path_segments=0`. Близость 23 218 units не была принята за доступ. Строка с точным отказом находится в `BAND_40_51_PROOF.tsv`; `REACHABLE` witness отсутствует. Новые NORMAL legs, topology edges и anchors не публиковались.

## Baseline и поверхность поиска

SHA-256 шести FINAL-GEO артефактов совпали с TASK: classification `2b7dc06855f873098c24180e9d3fe8a7dbb8dee46e1b342ecce8ab8d3469d311`, bridges `fa8665b2cbe9dbf62c0f75a7e3e4a4ef5dd868bbf128a186e05ac46abb096028`, closed `8036add79cb10acb00cf7145ac8200f6169733bf067d8fe87011363f18aea041`, reachability `27de6d2a6d0dc1f6a00dd6ce685b01d230676865bb05f04d512506d1e51a4158`, generated shard `7d92baaa21e40aa80010d7975ce64ab8050671a839fda49e0a6a2491cac47343`, manifest `75a73077b240ba50ccb38311acdd44a66867b59ab2e332b39e07883c84781b87`. D2 catalog: `5ccf7ac85e89027ba4506f20d64f41cc7167af285ceb88f1ef5a93f16cc5398f`, пять legs. Перед и после поиска GLOBAL band counts: `29/7/12/9/0/0/0/0/0`.

Machine filter `level_min<=51 && level_max>=40`: 563 ordinary groups, из них 136 `PUBLISHED_DISCONNECTED` в 73 компонентах, 216 `ANCHOR_BLOCKED_OPEN`, 211 `CLOSED_OR_MULTIFLOOR`, `ALREADY_REACHABLE=0`. Native NORMAL facts: 256 rows и 26 source spawns; в текущем admitted graph уже доступен один source spawn NPC 30540 с 12 native NORMAL destinations. NOBLESSE, special, instance и closed sources не использовались.

## GeoEngine и salvage

Штатный DB-free probe загрузил 203 geodata regions и 1 301 native doors. По выбранному компоненту Abandoned Coal Mines проверено 22 направленных candidates: 5 native destination connectors и 17 background bridges; все `NO_PATH`. По остальным компонентам выполнено 267 проверок (3 `VALID_DIRECT`, 15 `VALID_PATH`, 249 `NO_PATH`), затем 3 точечных farm-anchor проверки (`NO_PATH`). Итого 292 уникальных направленных проверки в текущем Goal. Детерминированные radii: 8 192 → 16 384 → 32 768 → 65 536; максимум новых checks на компонент 32, endpoint fanout не более 8 за волну. Никакой valid direction не составил полный направленный путь до ordinary 40–51 farm. 18 valid directions остались только локальным исследовательским evidence и не опубликованы. Логи и входы probe: `.phantom-local/logs/LIVE-002-BAND-40-51/`.

При первом поиске на компоненте `component.e04c7af9711cc5dd2a0ac987` рассматривались все его anchors, включая anchors других уровней. Это не дало band witness и не изменило их артефакты. Последующая проверка ограничила endpoints ровно band-farm anchors; дополнительных кандидатов в оставшемся бюджете оказалось 3, все `NO_PATH`. Поэтому результат ограничен указанным budget и не утверждает физическую невозможность всех потенциальных путей.

Для anchor salvage были отобраны четыре ближайшие open 40–51 C-blocked группы, у которых mapregion совпал с ближайшим native destination: все из Plunderous Plains, на расстоянии 14 288–31 557 units от Schuttgart destination. Canonical C proof уже проверил по 25 factual geometry candidates каждой: два `OUTSIDE_GEOMETRY`, два `PLANNER_TARGET_DISTANCE`. Все четыре сохраняют отказ при неизменном C geodata/geometry/planner contract; новых anchors не принято. Остальные open C-blocked groups не расширяли bounded search. Closed/multifloor 211 не рассматривались для salvage.

## Scope, проверки и доставка

Изменены только этот отчёт, `docs/phantoms/live-world/BAND_40_51_PROOF.tsv` и LIVE-002 checkpoint в `docs/phantoms/live-world/STATE.md`. D1/D2/topology production artifacts, Java, runtime, DB, rates, population, 52+, Ruins и Cat/Necro не изменены. Тестовый GeoEngine probe не поднимал LoginServer/GameServer и не инициализировал DB pool; его startup warnings об отсутствующем DB ожидаемы для DB-free helper. Независимый static пересчёт активного topology + admitted D2 catalog дал `GLOBAL_40_51_RECOMPUTED 0 PUBLISHED 136`; focused Python `test_final_geo`, `test_travel_backbone`, `test_normal_gk_catalog`: 14/14. Mojibake-маркеры в изменённых файлах проверены: 0. Escaped Cyrillic в изменённых файлах проверены: 0. `ant jar=0`, full `ant verify=0`, runtime/DB mutations=0. Две fresh byte-identical generation runs не заявляются: GREEN gate не достигнут.

Никаких изменений схемы БД, миграций, конфигов или production semantics. User dirty/untracked файлы сохранены. Риск: ограниченный spatial поиск не исключает другой factual маршрут, но публиковать его без GeoEngine proof нельзя. Следующий шаг для отдельного review: доказать именно недостающий destination→open farm GeoEngine path либо выбрать иной native NORMAL destination и подтвердить оба connector edges. 52–60 и другие blockers не начаты.

QUOTA_GUARD: около 193k Goal tokens на момент closeout, ниже hard checkpoint ~220k; поиск остановлен после exact `NO_GEODATA_PATH`. Git использован для требуемой проверки branch/HEAD/upstream/status, baseline и exact-path доставки. `git diff --check`, exact-path stage, commit и normal push выполняются после окончательной проверки этого отчёта; результат push и SHA сообщаются пользователю отдельно.

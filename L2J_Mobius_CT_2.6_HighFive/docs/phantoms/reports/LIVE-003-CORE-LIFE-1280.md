# LIVE-003-CORE-LIFE-1280 — CODE_GREEN_PARTIAL, 25.09.2026

Baseline `d4c4bfc5f4f9d0988251296147d2671a0aa84bd4`, branch `feature/phantom-world`. Final commit is recorded in the delivery message after the exact-path commit and push. `LIVE_003_0D_RUNTIME=PENDING_RUNTIME`; this report does not promote the live gate or raise either population target.

## Read-first and scope

Read the absolute LIVE-003 `TASK.md`, `EVIDENCE.md`, `ACCEPTANCE.md`; module `Agents.md`, master plan, workflow and task standards, ROADMAP/STATE, LIVE-003 0B/0B-C1/0C and LIVE-002 final reports; relevant portions of scheduler/activity, population/ecology/catalog/store, historical background/planner/model/operation/cursor, progression, materialization, topology authority, build targets and local DB-free suites. Module `README.md` and separate code-map/pattern files were not found in the narrow read pass. The accepted 0B admission and 0C recovery implementations were reused without edits. The user authorized this one large Goal; the bounded file exception contains only a nickname fallback, a pure aggregate suite/target, generated evidence, this report and STATE. Existing unrelated dirty files were preserved.

Owner map: logical presence belongs beside population/activity ownership, but has no integrated orthogonal axis; 5–15 minute due belongs in the shared `PhantomScheduler` with the historical background cursor, while current production background cadence is 10 seconds; native progression belongs to the current LIVE-002 knowledge/topology planner and progression service; location selection belongs at the materialization boundary with canonical topology; the weekly calendar belongs to PopulationEcology; nickname selection belongs to PopulationCatalog/Store; operator counters belong in existing `.phantomstatus`. No parallel life system was added.

## Completed code-side gate

The existing six nickname styles remain unchanged. `PhantomPopulationCatalog.fallbackName(profileId, attempt)` adds a deterministic alphanumeric continuation after the original eight candidates, and `PhantomPopulationStore` now permits 32 attempts on DB uniqueness collisions. The pure 10,000-profile run reserves all nine original candidates for 256 profiles and one fallback for 32 of them: 2,336 reserved fixture names, zero duplicate accepted names, zero exhausted profiles, deterministic two-run output, and all accepted names match the native 1–16 alphanumeric rule and the catalog reserved-token check. Actual accepted categories: CLEAN 3,120; COMPOUND 2,272; TRANSLIT_SLANG 1,904; DECORATED 1,148; DIGITS 865; LEET 435; NUMERIC_FALLBACK 256. Attempts 9 and 10 were exercised 224 and 32 times. `NICKNAME_CODE_GATE=GREEN`; `NICKNAME_LIVE_DB_GATE=PENDING_RUNTIME` because no DB profile creation was attempted.

The aggregate DB-free `phantom-live003-core-life-1280-test` also records the existing 0C restart-stable minute cursor and operation digest. This verifies reuse of an existing primitive, not periodic-due integration or transaction atomicity. A 10,000-profile pure priority-queue reference at 5–15 minutes produced 1,437,153 due events over 24 hours, mean 16.634/s, maximum 48/s and maximum queue 10,000; it instantiated no Players, DB profiles or per-profile schedulers. Production policy still reports `backgroundCadenceMillis=10000`, so B/C/I remain blocked. The reference rate is not a throughput claim.

## Current authority and calendar evidence

Read-only hashes match LIVE-002 final evidence: `high-five-generated-06.xml` SHA-256 `e55037269fec57dc37066c2f25f42d413216bc6a350ecc9bd7bfca55084c096b`, `FINAL_CONTENT_ACCESS_PROOF.tsv` SHA-256 `82a8b920d0e27697c35bf139d6e789f123ef8c37d908e0ea0c4522af07a437f3`. The prior LIVE-002 report established `LIVE002_DATA_COMPLETE=1`, Ruins GREEN and ordinary bands `5/13/15/3/1`. A fresh full authority loader run and Human/Dwarf/alternate-race route replay were not completed here; the D rows remain blocked. No fake route or conditional access was asserted.

The current ecology catalog was assigned to 1,280 synthetic LIVING profiles with the accepted deterministic seed and evaluated for every hour of a UTC reference week. Two runs yielded identical `LIVE003_WEEKLY_SCHEDULE_1280.tsv` bytes. ACTIVE ranges from 0 to 896; 92 of 168 hours have fewer than 64 ACTIVE candidates before admission fences. This proves `SCHEDULE_CAPACITY_INSUFFICIENT` for this reference composition. It does not establish an OFFLINE suppression rule; no logical OFFLINE state was integrated. The production calendar XML was left intact because its hash is pinned by persisted population/ecology state.

## Checkpoint status

`LIVE003_CORE_LIFE_MATRIX.tsv` is the exact A–I/0D ledger. A lacks an orthogonal AVAILABLE/BUSY/OFFLINE owner. B lacks integrated 5–15 minute due and complete interval rewards. C retains accepted 0C primitives but lacks periodic-due and materialization boundary proofs. D lacks native representative replan and capability-truth spoil proof. E lacks indexed local selection and hysteresis. F fails capacity and OFFLINE suppression. G code gate is GREEN. H has no new `.phantomstatus` counters. I is only a reference due-rate calculation, not a production scheduler/location-index contract. 0D remains PENDING_RUNTIME. No schema change was made.

## Verification and limits

Before and after edits, `ant compile-tests` and all nine requested safe regressions passed: `test-live003-admission`, `phantom-activity-scheduler-test`, `phantom-population-ecology-goal033-test`, `phantom-population-schedule-test`, `phantom-population-catalog-test`, `phantom-population-humanization-goal030a-test`, `phantom-background-model-test`, `phantom-progression-catalog-test`, `phantom-progression-operations-test`. The new `phantom-live003-core-life-1280-test` passed 4/4. Final combined Ant invocation exited 0 in 2m35s; only two pre-existing `runFinalization` deprecation warnings were emitted. `ant compile-tests` read the local HikariCP jar only on the compiler classpath; no Hikari initialization/pool, DB connection, GameServer/LoginServer or client was started. Guarded DB/background transaction/progression durability/production materialization targets were not run. `ant jar=0`, full verify=0, live scale=0. No runtime PopulationTarget or ActiveTarget change, LIVE-004/005 work or historical artifact mutation occurred.

This is a code-side partial result. A–F, H and I require a later bounded implementation/review gate before a CODE_GREEN claim; the existing runtime 0D gate remains separate. This Goal stops here.
